node_modules
dist
*.log

# Local scratch: request dumps and the local DNS workaround. Neither belongs in
# a deploy, and the log dump carries lead PII.
*.tmp
.dnsfix.mjs

# Secrets — never commit. Copy .env.example to .env instead.
.env
.env.*
!.env.example
!.env.test

# Local exports and dumps contain customer PII.
*.xlsx
*.dump
*.sql.gz
